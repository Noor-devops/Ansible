# - name: install and start nginx
#   hosts: frontend
#   become: yes # become root user 
#   tasks:
#   - name: install nginx
#     ansible.builtin.package: #to install package in ansible
#       name: nginx # required
#       state: present # required

#   - name: start nginx
#     service: #for services start (systemctl start service)
#       name: nginx
#       state: started
#       enabled: yes

- name: Installing nginx server
  hosts: frontend
  become: yes
  tasks: 
  - name: Installing nginx server
    package: 
      name: nginx
      state: present
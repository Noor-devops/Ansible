# - name: demo on loops
#   hosts: local
#   connection: local
#   tasks:
#   - name: print names
#     debug:
#       msg: "Hi {{ item }}" # item is reserved special variable
#     loop:
#     - Noor
#     - Afrin
#     - Tapsee

- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: print 1 to 100
    debug:
      msg: "printing numbers {{ item }}"
    loop: range(1,101) | list
